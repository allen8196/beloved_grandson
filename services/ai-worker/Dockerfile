# services/ai-worker/Dockerfile
# 這是一個佔位用的 Dockerfile，請根據實際的應用程式需求進行修改。

FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["python", "main.py"]
CMD ["tail", "-f", "/dev/null"]

```
.
├── 📜 README.md                  # 專案總覽、架構說明、環境設定與部署指南
├── 🐳 docker-compose.yml          # 用於本地開發環境，一鍵啟動所有服務與基礎設施
|
├── 📁 infra/                      # 基礎設施即程式碼 (Infrastructure as Code)
│   ├──  nginx/                   # Nginx 負載平衡器設定檔
│   │   └── default.conf
│   └── kubernetes/                # (可選) Kubernetes 部署描述檔 (YAMLs)
│       ├── web-app-deployment.yaml
│       ├── ai-worker-deployment.yaml
│       ├── llm-service-deployment.yaml
│       └── ... (其他服務的部署檔案)
|
├── 📁 services/                   # 核心應用程式與微服務的原始碼
│   │
│   ├── 🌐 web-app/                # Flask Web App (API / 前端請求處理)
│   │   ├── app/                   # Flask 應用程式主要模組
│   │   │   ├── __init__.py        # 應用程式工廠
│   │   │   ├── api/               # API 藍圖 (Blueprints)
│   │   │   │   ├── tasks.py       # 處理任務創建、狀態查詢的端點
│   │   │   │   └── users.py       # 處理使用者認證、資料的端點
│   │   │   ├── core/              # 核心商業邏輯與服務
│   │   │   │   ├── task_service.py # 封裝與 RabbitMQ 和 Redis 的互動邏輯
│   │   │   │   └── user_service.py # 使用者相關邏輯
│   │   │   ├── models/            # 資料庫模型 (SQLAlchemy)
│   │   │   │   ├── user.py
│   │   │   │   └── conversation.py
│   │   │   ├── static/            # 靜態檔案 (CSS, JavaScript)
│   │   │   └── templates/         # HTML 樣板 (Jinja2)
│   │   ├── config.py              # 應用程式設定 (資料庫連線、金鑰等)
│   │   ├── main.py                # 應用程式啟動入口
│   │   ├── Dockerfile             # 用於建構此服務的 Docker 映像檔
│   │   └── requirements.txt       # Python 依賴套件
│   │
│   ├── 🤖 ai-worker/              # 基礎版 AI Worker (非同步任務處理)
│   │   ├── worker/                # Worker 程式碼
│   │   │   ├── __init__.py
│   │   │   ├── tasks.py           # 定義從 RabbitMQ 接收的任務邏輯
│   │   │   └── clients/           # 用於與其他服務溝通的客戶端
│   │   │       ├── stt_client.py
│   │   │       ├── llm_client.py
│   │   │       └── tts_client.py
│   │   ├── main.py                # Worker 啟動入口，監聽 RabbitMQ 佇列
│   │   ├── Dockerfile             # 用於建構此服務的 Docker 映像檔
│   │   └── requirements.txt       # Python 依賴套件
│   │
│   ├── 🎙️ stt-service/           # 語音轉文字 (STT) 微服務
│   │   ├── api/                   # API 介面 (可使用 FastAPI 或 Flask)
│   │   ├── models/                # 載入與執行 STT 模型的邏輯
│   │   ├── main.py                # 服務啟動入口
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   │
│   ├── 🧠 llm-service/            # 大型語言模型 (LLM) 微服務
│   │   ├── api/
│   │   ├── rag/                   # RAG 相關邏輯 (與 Milvus 互動)
│   │   ├── llm/                   # TAIDE 模型載入與互動邏輯
│   │   ├── main.py
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   │
│   └── 🔊 tts-service/            # 文字轉語音 (TTS) 微服務
│       ├── api/
│       ├── models/
│       ├── main.py
│       ├── Dockerfile
│       └── requirements.txt
|
└── 📁 scripts/                     # 各類輔助腳本
    └── rag_ingestion/             # 離線 RAG 資料載入腳本
        ├── main.py                # 執行資料處理與載入的主腳本
        ├── data_loader.py         # 從來源讀取資料
        ├── text_processor.py      # 文字清洗、切割
        ├── vectorizer.py          # 將文字轉換為向量
        └── milvus_loader.py       # 將向量寫入 Milvus

```
